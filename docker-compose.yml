services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: arbeitskleidung
      POSTGRES_PASSWORD: arbeitskleidung
      POSTGRES_DB: arbeitskleidung
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data

volumes:
  db-data:
